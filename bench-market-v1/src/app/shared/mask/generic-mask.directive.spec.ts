import { GenericMaskDirective } from './generic-mask.directive';

describe('GenericMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new GenericMaskDirective();
    expect(directive).toBeTruthy();
  });
});
